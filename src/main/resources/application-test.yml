server:
  port: 8080

spring:
  # 数据库连接配置
  datasource:
    url: jdbc:mysql://39.106.120.32:3306/college_entrance_examination_volunteer_consultation?useUnicode=true&characterEncoding=UTF-8&useSSL=false&allowPublicKeyRetrieval=true
    username: chenhang
    password: Chh980929...
    driver-class-name: com.mysql.cj.jdbc.Driver

# mybatis的相关配置
mybatis:
  #mapper配置文件
  mapper-locations: classpath:mapper/*.xml
  #开启驼峰命名
  configuration:
    map-underscore-to-camel-case: true